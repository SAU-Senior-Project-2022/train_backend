version: "3.9"
services:
  flask:
    build: .
    ports:
      - "5000:5000"
    links:
      - mariadb
  mariadb:
    image: mariadb:10.3.32-focal
    environment:
      MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: "yes"
      MARIADB_DATABASE: "train"
    ports:
      - "3307:3306"
